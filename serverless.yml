service: exchange-service

useDotenv: true

package:
  individually: true

provider:
  name: aws
  runtime: nodejs18.x
  stage: dev
  region: us-east-1

functions:
  app:
    handler: dist/lambda.handler
    events:
      - http:
          path: "{proxy+}"
          method: any
